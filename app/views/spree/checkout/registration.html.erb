<%= render :partial => 'spree/shared/error_messages', :locals => { :target => @user } %>
<h1><%= t(:registration) %></h1>
<div id="registration" data-hook>
  <div id="account" class="span6">
    <!-- TODO: add partial with registration form -->
  </div>
  <% if Spree::Config[:allow_guest_checkout] %>
    <div id="guest_checkout" data-hook class="span6">
      <%= render :partial => 'spree/shared/error_messages', :locals => { :target => @order } %>
      <h4><span class="label label-success"><%= t(:guest_user_account) %></span></h4>
      <%= form_for @order, :url => update_checkout_registration_path, :method => :put, :html => { :id => 'checkout_form_registration' } do |f| %>
        <p>
          <%= f.label :email, t(:email) %><br />
          <%= f.email_field :email, :class => 'title' %>
        </p>
        <p><%#= f.submit t(:continue), :class => 'btn btn-primary' %>
          <button type="submit" class="btn btn-primary">
            <i class="icon-arrow-right icon-white"></i> <%= t(:continue) %>
          </button>
        </p>
      <% end %>
    </div>
  <% end %>
</div>
